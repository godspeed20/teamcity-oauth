package jetbrains.buildServer.auth.oauth;

public enum ConfigKey {
    preset,
    authorizeEndpoint,
    tokenEndpoint,
    userEndpoint,
    rolesEndpoint,
    clientId,
    clientSecret,
    scope,
    hideLoginForm,
    allowInsecureHttps
}
